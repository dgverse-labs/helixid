// AgentWallet — encrypted local storage of agent key, DID, and VC.
// SA-1: the private key is generated locally and never transmitted.
// Encryption: AES-256-GCM with PBKDF2 key derivation (see decisions.md).
export class AgentWallet {
  // placeholder — full implementation in Story 4
}
